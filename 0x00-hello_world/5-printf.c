#include <stdio.h>
/**
 * main - program entry point
 * Return: value 0 program  successful
 */
int main(void)
{
	        printf("with proper grammar, but the outcome is a piece of art,\n");
		        return (0);
}
